import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
  
    important: {
      color: 'red',
      fontSize: 30
    },
  
    box: {
      borderWidth: 1
    }
  });

  export { styles };